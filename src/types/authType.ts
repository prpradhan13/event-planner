export type EmailPassword = {
    email: string;
    password: string;
}

export type UserTypes = {
    id: string;
    email: string;
    full_name: string;
    username: string;
    avatar_url: string | null;
}